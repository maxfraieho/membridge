# membridge — MinIO sync configuration
# Copy to config.env and fill in your credentials:
#   cp config.env.example ~/.claude-mem-minio/config.env
#   ln -sf ~/.claude-mem-minio/config.env config.env

# MinIO / S3-compatible endpoint
MINIO_ENDPOINT=https://your-minio-endpoint.example.com
MINIO_ACCESS_KEY=your-access-key
MINIO_SECRET_KEY=your-secret-key
MINIO_BUCKET=claude-memory
MINIO_REGION=us-east-1

# Project identity — MUST be identical on all machines
# canonical_id is always computed as sha256(CLAUDE_PROJECT_ID)[:16]
CLAUDE_PROJECT_ID=mem

# Local SQLite DB path
CLAUDE_MEM_DB=$HOME/.claude-mem/claude-mem.db

# Distributed lock TTL (seconds)
LOCK_TTL_SECONDS=7200

# Emergency lock override (set to 1 only when needed)
FORCE_PUSH=0
